```console
# Run the tests
behave
behave --no-capture --no-capture-stderr --no-color
behave --name <Scenario name>

# https://stackoverflow.com/questions/41146633/how-to-integrate-behave-into-pytest
# Run the tests with pytest
pytest
```
